<!DOCTYPE html>
<html>

<head>
    <%- include('../../partials/head.ejs') %>
        <style>
            .container {
                margin-top: 60px;
                margin-bottom: 20px;
                padding-top: 14px;
                display: flex;
                flex-direction: column;
                gap: 20px
            }

            .insertLabel {
                height: 34px;
                display: flex;
                justify-content: space-between;
                gap: 116%;
            }

            .table-wrapper {
                width: 100%;
                overflow-x: auto;
                box-shadow: 0 2px 4px rgba(0, 0, 0, .1), 0 8px 16px rgba(0, 0, 0, .1);
            }

            table {
                min-width: 1400px;
            }
        </style>
</head>

<body>
    <div class="container table-wrapper">
        <div class="insertLabel">
            <select class="form-select form-select-lg mb-3 pageCount" aria-label=".form-select-lg example" id="limit"
                onchange="loadWardenPage()">
                <option value="5" href="#">5</option>
                <option value="10" href="#" selected>10</option>
                <option value="20" href="#">20</option>
                <option value="40" href="#">40</option>
            </select>
            <a class="btn btn-info" href="/warden/add">Add</a>
        </div>
        <table class="table table-striped table-hover table-bordered">
            <thead>
                <tr>
                    <th scope="col">Sno</th>
                    <th scope="col">Name</th>
                    <th scope="col">DOB</th>
                    <th scope="col">EmailId</th>
                    <th scope="col">CreatedAt</th>
                    <th scope="col">CreatedBy</th>
                    <th scope="col">UpdatedAt</th>
                    <th scope="col">UpdatedBy</th>
                    <th scope="col">DeletedAt</th>
                    <th scope="col">DeletedBy</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody id="wardensData">
                <tr>
                    <td>
                        <div class="d-flex justify-content-center">
                            <div class="spinner-border" role="status">
                                <span class="visually-hidden"></span>
                            </div>
                        </div>
                    </td>
                </tr>
            </tbody>
        </table>
        <nav aria-label="Page navigation example">
            <ul class="pagination justify-content-center" id="pagination">
                <li class="page-item disabled" id="previous-page">
                    <a class="page-link" href="#" tabindex="-1" aria-disabled="true">Previous</a>
                </li>
                <li class="page-item"><a class="page-link" href="#">1</a></li>
                <li class="page-item"><a class="page-link" href="#">2</a></li>
                <li class="page-item"><a class="page-link" href="#">3</a></li>
                <li class="page-item" id="next-page">
                    <a class="page-link" href="#">Next</a>
                </li>
            </ul>
        </nav>
    </div>
</body>
<script>
    var tableData = document.getElementById("wardensData")
    var page = document.getElementById("pagination").value
    var currentPage = 1
    
    function loadWardenPage() {
        var limitSelect = document.getElementById("limit").value

        var requestOptions = {
            method: 'GET'
        };

        fetch(`http://localhost:1000/api/warden?limit=${limitSelect}&page=${page}`, requestOptions)
            .then(response => response.json())
            .then(wardens => {
                var trHtml = ''
                for (var i = 0; i < wardens.length; i++) {
                    var warden = wardens[i]
                    trHtml = trHtml + `<tr>
                            <td>${i + 1}</td>
                            <td>${warden.name}</td>
                            <td>${warden.dob}</td>
                            <td>${warden.emailId}</td>
                            <td>${warden.createdAt}</td>
                            <td>${warden.created}</td>
                            <td>${warden.updatedAt}</td>
                            <td>${warden.updated}</td>
                            <td>${warden.deletedAt}</td>
                            <td>${warden.deletedBy}</td>
                            <td>
                                <a class="btn btn-secondary" href="/warden/${warden.wardenId}">Edit</a>
                                <a class="btn btn-secondary" onClick="deleteWarden(${warden.wardenId})">Delete</a>
                            </td>
                        </tr>`
                }
                tableData.innerHTML = trHtml
                var totalPages = Math.ceil(warden.totalWardens / limitSelect)
                var paginationHtml = ''
                var pagination = document.getElementById("pagination")

                for (var i = 1; i <= totalPages; i++) {
                    paginationHtml += `<li class="page-item">
                          <a class="page-link" href="#" onclick="loadWardenPage(${i})">${i}</a>
                       </li>`;
                }
                pagination.innerHTML = paginationHtml;
            })
            .catch((error) => console.error(error));
    }

    function deleteWarden(wardenId) {
        var validateDelete = confirm('are you sure you want to delete?')

        if (!validateDelete)
            return

        var requestOptions = {
            method: 'DELETE',
        };

        fetch("http://localhost:1000/api/warden/" + wardenId, requestOptions)
            .then(response => response.text())
            .then(deleted =>
                window.location = '/warden'
            )
            .catch(error => console.log('error', error));
    }

    window.addEventListener('load', () => {
        loadWardenPage()
    })

</script>

</html>